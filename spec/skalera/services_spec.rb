require 'spec_helper'

describe Skalera::Services do
  it 'has a version number' do
    expect(Skalera::Services::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
